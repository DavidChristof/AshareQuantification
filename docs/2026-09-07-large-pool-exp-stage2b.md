# 2026-09-07 实验 · 阶段二B：全 599 宽度 v2 集成 —— GPU 完整集成终比

> 继 stage2a（GBM go/no-go）。装上 CUDA torch 后用 **完整 v2 集成（lstm+transformer+gbm）**、
> **GPU(RTX4060)** 在 ~2.7 年窗口做 40 vs 599 终比。
> **结论更新**：600 池训练的完整集成明显更稳更强（**ICIR 0.407 vs 40池 0.135**，三个成员全为正）；
> 此前"近350日 gbm 为负"是**短窗口伪影**。**线上 results/model_v2 仍未动**，切换还需生产级重训+推理口径改造。

## 关键结果（2026-09-07 深夜，GPU）

| 训练截面 | 窗口 | 模式 | 验证日 | RankIC | ICIR | 分成员 RankIC (lstm/trf/gbm) |
|---|---|---|---|---|---|---|
| base40 | 近700日(~2.7y) | 完整集成 | 133 | 0.0308 | 0.135 | −0.004 / −0.039 / **+0.045** |
| **full599(40+559)** | **近700日(~2.7y)** | **完整集成** | **135** | **0.0362** | **0.407** | **+0.027 / +0.027 / +0.039** |

要点：
1. **完整集成下，600 池全面占优**：集成 ICIR 0.407 vs 0.135；且三个成员在 600 池上**全部为正**，
   而 40 池上 lstm/transformer 为负（深度模型在窄/同质截面退化成噪声）——印证"截面太窄"是 DL 成员失效主因。
2. 补全此前各口径（含全历史 gbm：base40 −0.011 → large50 +0.033；近350日 gbm：base +0.047 vs full599 −0.020）
   均落 `results/large_pool_gbm_compare.json`。近350日的负值在拉长窗口 + 完整集成后不再出现 → 属短窗/单成员伪影。
3. 运行时：torch 2.13.0+cu126（官方源，晚间曾限速；下载完成 2474MiB），`make_samples` 峰值 ~2.4GB(近700日×599)，
   full599 完整集成 epochs6 耗时 **319s（GPU）** —— 重训已从"小时级(CPU全历史)"降到"分钟级"，可常态重训。

## 已知局限（诚实记录）

- **窗口 ~2.7 年（recent700）而非全历史**：全历史 × 599 的 X≈5.7GB 仍超出本机可用内存，未跑。
- **quick 参数**：epochs=6、gbm=400 树（生产 config 为 50 epochs / 800 树）；方向可信、绝对量待生产级复跑。
- **推理口径**：600 训练模型的概率 = "跑赢当日 600 池中位数"；若要上线，API 端 `DATA`/打分截面须扩到
  600 池（现只有 40），语义才一致 —— 属**上线前置改造**，需单独评审。

## 下一步（待用户定）

1. **生产级复跑**：GPU 上以 config epochs/树数，对 base40 vs full599（近700日或腾 RAM 后全历史）再跑一次定案。
2. 通过后：① 训练时新增 `--universe large` 落成正式重训路径（scripts/18）；② API 推理 DATA 扩到 600；
   ③ 再谈"在榜但 prob 低是否减持"等下游。
3. **全程 results/model_v2（线上）未动**。
