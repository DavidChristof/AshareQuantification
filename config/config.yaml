data:
  source: akshare
  db_path: data/market.db
  csv_dir: data/csv
  start_date: '2020-01-01'
  end_date: '2026-08-31'
  universe:
  - '000301'
  - '000333'
  - 000408
  - 000792
  - 000807
  - 000975
  - 000977
  - '002001'
  - '002027'
  - '002532'
  - 002558
  - '002602'
  - 002648
  - 002709
  - '300628'
  - '300750'
  - '300760'
  - '300866'
  - '301308'
  - '600066'
  - '600160'
  - '600489'
  - '600519'
  - '600549'
  - '600600'
  - '600809'
  - '600989'
  - '601138'
  - '601336'
  - '601600'
  - '601628'
  - '601872'
  - '601899'
  - '603259'
  - '603369'
  - '603986'
  - '603993'
  - '605117'
  - '605499'
  - '688111'
  universe_names:
    '000301': 东方盛虹
    '000333': 美的集团
    000408: 藏格矿业
    000792: 盐湖股份
    000807: 云铝股份
    000975: 山金国际
    000977: 浪潮信息
    '002001': 新和成
    '002027': 分众传媒
    '002532': 天山铝业
    002558: 巨人网络
    '002602': 世纪华通
    002648: 卫星化学
    002709: 天赐材料
    '300628': 亿联网络
    '300750': 宁德时代
    '300760': 迈瑞医疗
    '300866': 安克创新
    '301308': 江波龙
    '600066': 宇通客车
    '600160': 巨化股份
    '600489': 中金黄金
    '600519': 贵州茅台
    '600549': 厦门钨业
    '600600': 青岛啤酒
    '600809': 山西汾酒
    '600989': 宝丰能源
    '601138': 工业富联
    '601336': 新华保险
    '601600': 中国铝业
    '601628': 中国人寿
    '601872': 招商轮船
    '601899': 紫金矿业
    '603259': 药明康德
    '603369': 今世缘
    '603986': 兆易创新
    '603993': 洛阳钼业
    '605117': 德业股份
    '605499': 东鹏饮料
    '688111': 金山办公
features:
  window: 30
  horizon: 5
  technical:
  - ma
  - rsi
  - macd
  - volatility
  - volume_ratio
model:
  type: lstm
  hidden_size: 64
  num_layers: 2
  dropout: 0.2
  lr: 0.001
  epochs: 50
  batch_size: 64
  patience: 8
  seed: 42
  device: auto
# 横截面增强模型（v2，2026-09-02 升级：相对标签 + 截面/Alpha101 特征 + 多模型集成）
# 根因：旧 LSTM 学"单股绝对涨跌"≈抛硬币(acc~0.5)，与"选股=横截面排序"任务错配。
# 有该段时 API/重训优先使用 v2；想回退旧模型，删除/改名 results/model_v2 即可。
model_v2:
  enabled: true
  dir: results/model_v2
  label_mode: relative        # relative = 未来 horizon 跑赢全池当日中位数(截面) | absolute = 未来绝对涨跌
  cross_features: true        # 加当日截面分位特征 rank_close/rank_volume/rank_ret1/rank_ret5
  alpha_features: true        # 加 Alpha101 + 自动挖掘精选因子（与 17_factor_mine 共享算子库）
  members: [lstm, transformer, gbm]   # 集成成员：lstm / transformer / gbm（至少 1 个）
  gbm:
    n_estimators: 800         # LightGBM（缺失时回退 sklearn HistGradientBoosting）
    learning_rate: 0.05
    num_leaves: 63
    max_depth: 8
    min_child_samples: 40
    subsample: 0.9
    colsample_bytree: 0.8
backtest:
  initial_capital: 100000.0
  commission: 0.0003
  slippage: 0.0002
  stamp_tax: 0.0005    # 印花税（卖出单边，2023.8 起 0.05%）
  threshold: 0.55
  position_pct: 0.95
  risk_free: 0.02      # 无风险利率（年化），用于夏普/Alpha/Treynor/Sortino
manual:
  initial_capital: 100000.0
  db_path: paper/manual_account.db
  max_order_shares: 10000
  enforce_trading_hours: true   # 闭市/休市禁止下单（仅交易日 9:30-11:30 / 13:00-15:00）
  lot_size: 100                # A股买入整手（100 股整数倍）
  # 自动止盈止损巡检（2026-09-17 从两个 GET 接口搬进独立线程，见 docs/2026-09-17-duplicate-auto-sell.md）
  # 原来它挂在 /api/manual/account 与 /api/manual/positions 上，看板一轮询就真下单
  # ——触发频率秒级，把 sell() 的竞态放大成了必然（同一笔 400 股被卖 800 股）。
  auto_stops: true             # 关掉则完全不自动止盈止损（只保留手工下单）
  stop_check_interval_sec: 30  # 巡检间隔；只在交易时段真正执行
# 实盘炒股账户（本金 ¥3000 · 模型只给「建议」· 人工在券商 App 下单后回来记账）。
# ⚠️ 程序绝不自动下单、不接入任何券商 API（故无需程序化交易报备）；本段与上面两个账户完全隔离。
real:
  enabled: true
  initial_capital: 3000.0
  db_path: paper/real_account.db
  lot_size: 100                # A股整手（买入须 100 股整数倍）
  max_order_shares: 2000       # 单笔上限（¥3000 本金下足够）
  enforce_trading_hours: true  # 只约束「建议」时效；记账不受限（成交回报常在盘后补录）
  # ---- 仓位政策：¥3000 只够 2 只；默认组合逻辑的 5 只/单票20% 完全不适用 ----
  max_positions: 2             # 持仓上限
  target_position_pct: 0.95    # 目标总仓位
  max_stock_pct: 0.50          # 单票市值上限（2 仓 → 每只 ≤50%）
  single_position_pct: 0.90    # 仅空仓且无第二只可负担时，放宽单只上限（例外，界面会注明）
  # ---- 费用（真实口径：单笔最低佣金 ¥5 是 ¥3000 账户的主要成本）----
  commission: 0.0003           # 佣金 万3
  min_commission: 5.0          # 单笔最低佣金 ¥5（买卖各收一次）
  stamp_tax: 0.0005            # 印花税（卖出单边 0.05%）
  transfer_fee: 0.00001        # 过户费（双边 0.001%）
  slippage: 0.0                # 按人工回报的成交价记账，不加滑点
  # 往返费用需涨幅 > 该值 → 不建议（低价股/小额单的费用占比过高）。
  # 2026-09-11 收紧 1.5 → 1.2：实测（554 只可买票 × 43 万笔）毛均收益仅 +0.96%，
  # 而往返费用拖累 1.82pp（¥729 小单 = 1.42%，几乎全是"最低佣金 ¥5"）——费用才是净收益
  # 的主要杀手，且与止损宽窄无关。故按费用效率过滤：约名义 ≥¥870（1 手 ≥¥8.7 或 2 手 ≥¥4.35）。
  max_breakeven_pct: 1.2
  min_order_amount: 500.0      # 单笔金额下限（低于此费用占比过大）
  # 成交可行性判定（严格档）——阈值见 quant/trading/fill.py:FillConfig
  fill:
    drift_tol_pct: 1.0         # 现价 vs 参考价漂移 > 1% → 需改价方能成交（hard）
    miss_tol_pct: 2.0          # 漂移 > 2% → 已错过
    touch_tol_pct: 0.3         # 委托价贴近当日已触及区间边缘 → 降级 likely
    near_limit_pct: 0.5        # 距涨跌停 ≤0.5% → 封板风险
    depth_ratio: 1.0           # 盘口量/建议股数 < 该值 → 可能部分成交
    open_grace_min: 5          # 开盘后前 N 分钟漂移阈值放宽（集合竞价跳空）
  # 建议参数（stop/take 为实盘建议线；空表示沿用全局 risk 段）
  advice:
    n: 3                       # 最多输出 3 条买入建议（2 仓位 + 备选）
    # 入场择时（只改「何时下手」，不改「选谁」）：现价高于 5 日均线 → 不追，标成「等回踩」。
    # 实证（552只·2020-2026·顶部12只·T+1开盘买持5日）：加闸门后均收益 0.06%→0.50%，
    # 7 年中 6 年有效、两个半段均有效（2023 年是唯一失效年 -0.37% vs -0.09%），属
    # 「多数时候有帮助的温和杠杆」而非稳赚；故不硬过滤，只降级为"等回踩"并给出触发价。
    entry_gate: true
    stop_loss_pct: 0.08
    take_profit_pct: 0.10
    trailing_pct: 0.08
    use_selection: true        # 候选来源 = 每日选股 topN（同组合调仓）；false = 40 只模型池
auto_refresh:
  enabled: true
  check_interval_minutes: 30
  # 收盘后刷新 40 池（market.db）。**不能早于行情源发布当日日线的时间** —— 太早只会
  # 拿到 0 行、白跑一次。失败时调度器每 check_interval 分钟自动重试（`_last_updated`
  # 只在数据真推进到新交易日时才更新，见 api/main.py::_run_auto_update）。
  # 实测 2026-09-14：行情源 **16:41~16:43** 才发布当日日线（16:40:06 仍只有 09-11、
  # 16:43:07 已有 09-14）。故取 16:45（发布时刻会有一两天波动，靠重试兜底）。
  update_time: '16:45'
  auto_rebalance: true
  # 收盘后自动维护 600 池 + 影子 A/B（scripts 26→27→28）：服务在 run_time 时刻挂着才跑，
  # 没挂当日跳过（符合“本地无法 24h 挂服务”约束）。run_time 需在 40 池刷新 (update_time)
  # 之后 —— 先保证 market.db 的 40 只今天已就绪，26 补大池、27 才能在完整 ~599 截面上打分。
  shadow_ab:
    enabled: true
    # 触发时刻。设在行情源发布当日日线**之后**（2026-09-14 实测发布 ≈16:43，故取 16:55）。
    # ⚠️ 这个值不必精确：worker 每轮**先探「40 池是否已推进到今天」**，没推进就只睡
    # retry_interval 分钟重试、**跳过昂贵的 26→27→28**（559 次请求 + 约 7 分钟）；
    # 发布后才跑，且跑完还要校验大池库确实推进。
    run_time: '16:55'
    grace_min: 120        # 触发点过后仍允许补跑（服务开晚了也在当天内补）；超过则当日跳过
    # 行情源的「当日日线」在收盘后一段时间才发布（实测 2026-09-14 ≈16:43；
    # 2026-09-11 是 15:46 拿 0 行、16:55 才拿到）→ 先探源、没发布就等一会重试；
    # **只有校验通过才写 logs/shadow_ab_date 标记**（否则兜底定时会误判"服务已跑"而跳过补跑）。
    retry_interval_min: 10   # 未拿到当日日线 / 内存不足时的重试间隔（分钟）
    retry_until: '18:30'     # 重试截止时刻；到点仍未拿到则本日不写 marker，留给兜底定时
    # 内存闸门：可用物理内存低于此值就**不启动** 26→27→28，等下一轮重试。
    # 26 用 akshare 新浪日线，每个 worker 起一个 py_mini_racer(V8) 上下文，V8 启动要预留
    # partition 地址空间 —— 内存紧张时直接 [FATAL:partition_address_space.cc(243)] 进程级
    # 崩溃，崩了就是 0 行、白跑一轮。实测 2026-09-10：可用 854MB 时 workers=4 秒崩；
    # 2026-09-14：可用仅 400MB（开着游戏）时 workers=2 也在 7 秒内崩。
    min_free_mb: 1200
    # scripts/26 的大池拉取并发。akshare 新浪日线每 worker 起一个 py_mini_racer(V8) 上下文，
    # 并发 >1 会触发 V8 FATAL(partition_address_space) 进程级崩溃，崩了就是 0 行白跑一轮。
    # ⚠️ 2026-09-16 实测修正：**内存充足也照崩** —— 当时可用 3650MB（远高于 min_free_mb=1200），
    #    workers=2 仍在 7 秒内崩（`logs/shadow_ab_2026-09-16.log` 有完整栈）。
    #    昨天同样的脚本 `--workers 1` 跑完 559/559。⇒ **min_free_mb 挡不住这件事，
    #    并发数才是关键**，故从 2 降到 1。（原注释把根因归给内存，是错的。）
    workers: 1
    recent: 700           # scripts/27 每只取最近 N 交易日打分（控耗时）
    # ---- 全市场日线增量（scripts/35 --refresh）----
    # 为什么接进来：`data/full_market.db` 是 PIT 无偏宇宙的**数据底座**，此前没接入任何
    # 定时任务 ⇒ 库**冻结在 2026-09-11**，所有 PIT 结论（scripts 36/37/40/43）都停在那个
    # 日期。接上之后，前向样本才会随日子自己长起来（PIT 快照从 2021-06 起，回测样本最多
    # 5.25 年；而按实测 se 反推要 ~7 年才能把效应确立到 |t|>2 —— 所以「让它自己长」是唯一
    # 能补上这个缺口的办法）。
    # [!] 代价与风险（与 scripts/35 自己的建议冲突，如实记下）：
    #   scripts/35 走 akshare，**每次调用新建一个 V8(py_mini_racer) 上下文**，而它的
    #   docstring 写着「跑之前建议先停掉 8001 服务」。这里是**在服务运行时**跑 —— 属于
    #   明知有风险仍接。三层缓解：① 子进程隔离（V8 FATAL 只杀子进程，服务不受影响）；
    #   ② 本步失败**不中断**流水线（只 warn，26→28 照跑）；③ 更严的内存闸门（见下）。
    #   跑一次约 5219 次请求，按 min_gap 0.4s / 2 workers 约 17 分钟。
    #   要整体关掉：full_market.enabled: false
    full_market:
      # 2026-09-22 首次实跑挂死 -> 加看门狗/定期 flush 后，2026-09-23 复跑**完整跑完**：
      #   成功 5218/5218、失败 0、**看门狗 0 次触发**、用时 61.6 分钟；
      #   库推进 09-11 -> **09-22**（+36,304 行），5186 只到 09-22、其余是停牌股。
      # 挂死根因已辨明：**不是坏票、也不是限流**（两个假设都被这次复跑否掉 ——
      #   若是坏票，同一只票会再挂、看门狗必然触发；若是限流，会在同样的请求数处再停）。
      #   => 是**单次请求的偶发网络挂起**，而 workers=1（V8 逼的）把它放大成了「整个循环全停」。
      #   看门狗正对这个场景。注意脚本头虽有 socket.setdefaulttimeout(25) 但**没兜住**。
      enabled: true
      workers: 1          # [!] 2026-09-22 实测：workers=2 直接崩在 V8（py_mini_racer 栈），
                          # 与 scripts/26 同一种病、且同样与内存无关（当时可用 5.4GB）。
                          # workers=1 冒烟 20/20 通过。**不要调大。**
      weekdays: '4'       # 只在周五跑（0=周一 … 4=周五）。为什么不是每天：
                          #   [!] 2026-09-22 实测：workers=1 + min_gap 0.4 => 5218 只约 **2 小时**
                          #   （100 只用 2.3 分钟；每请求 0.4s 间隔 + 约 1s 网络）。
                          #   每天跑不现实；而 --refresh 每股是**追加其最后一行之后的所有行**，
                          #   所以每周跑一次拿到的**数据是完整的**（只是抓取频率低），
                          #   请求量降到 1/5（5218/周 vs 5218/天）。
                          #   它排在流水线**最后**（见 api/main.py::_SHADOW_STEPS）——
                          #   两小时的步骤若放前面会把 26→28 整体推迟。
      min_free_mb: 2500   # 比上面的 min_free_mb(1200) 更严：35 每次调用都新建 V8 上下文
      min_gap: 0.4        # 请求间隔（秒），与 35 的稳定档一致
selection:
  auto: true        # 每日选股：收盘后随自动刷新运行（寻找大盘优质股）
  n: 12             # 展示优质股数量
  universe: large   # 候选宇宙：large=现池40∪中小盘缓存~560（600只实证低波动/反转所在）| hs300=仅沪深300
  # regime 门控：按市场状态(沪深300近20日趋势)切换技术面因子权重——实证(29号)价量 alpha 是
  # 条件性的：下跌市 低波+60日反转 最强(RankIC0.12/超额+1.4%)，上涨市 低波/反转 负暴露 → 动量主导。
  # 权重表见 quant/data/selector.py: TECH_REGIME_WEIGHTS；false = 恒用默认(动量25/趋势15/低波30/反转30)
  regime_gating: true
realtime:
  enabled: true
  interval_seconds: 10
  sources:
  - sina
  - tencent
minute:
  enabled: true
  scale: 5
  refresh_interval: 60
  datalen: 1023
  db_path: data/minute.db
  concurrency: 4
trading:
  max_positions: 5
  # 自动纸面盘「多日持有」双阈值（quant/trading/engine.py）：
  #   掉出核心榜但概率仍 ≥ clear_threshold → 保留 hold_trim 成观察仓（部分减持，不清仓）
  #   跌破 clear_threshold（或无当日信号）→ 全仓卖出
  #   设 null 回到旧「掉榜即清」模式（每期整仓轮动）
  clear_threshold: 0.45
  hold_trim_pct: 0.5
  minute_adj: 0.05   # 盘中分钟模型对综合分的修正强度（方向化，buy +0.05 / sell -0.05）
  # 组合/一键调仓的「交易候选」来源：
  #   selection = 今日选股 topN（600 只大池，基本面+低波动+60日反转实证因子）——主推
  #   model40   = 旧逻辑：40 只训练池按模型综合分 topN（可随时回退）
  candidate_source: selection
portfolio_risk:
  market_weak_threshold: -1.0   # 大盘(沪深300)当日跌幅低于此值(%) → 弱势，降仓
  weak_position_pct: 0.5        # 弱势时总仓位上限（占资产比例）
  max_stock_pct: 0.20           # 单票仓位上限（避免重仓单票）
  # 分钟否决买入：默认关闭！分钟模型概率极端未校准（常 0.002~0.006 恒判 sell），
  # 若开启会把盘中所有买入目标都 skip（一键调仓"卖出能成交、买入全被拦"）。
  # 待分钟模型校准/重训后再置 true；期间靠 大盘弱势降仓 + 单票上限 兜底。
  minute_veto: false            # true=盘中分钟 sell 暂停买入该目标 | false=只做方向化微调(±minute_adj)
  # ---- 账户回撤熔断（2026-09-11，看"自己亏多少"而非"大盘跌多少"）----
  # 净值自近 window_days 高点回撤 ≥ trip_pct → 总仓位降到 position_pct 且停开新仓；
  # 回撤收窄到 release_pct 以内解除（滞回防抖，状态持久化 logs/drawdown_brake.json）。
  # 实证（600池top12等权·2020-2026·含换手费）：最大回撤 −51.4% → −32.5%、年化 −0.7% → +0.9%；
  # 再叠加弱势减半 → +1.3%/Sharpe 0.18。更紧的 6%/3% 并不更好（8%/4% 为本样本最佳）。
  drawdown_brake:
    enabled: true
    window_days: 60
    trip_pct: 8.0          # 回撤 ≥8% 触发
    release_pct: 4.0       # 回到 4% 以内解除（滞回区 [4%,8%) 维持原状态）
    position_pct: 0.5      # 触发期间总仓位上限
    block_new_buys: true   # 触发期间不开新仓
  # 大盘趋势闸门（2026-09-11 引入，**2026-09-18 关闭**）：指数跌破 N 日均线 → 当日不开新仓。
  # 关闭理由（三次独立证据，详见 docs/2026-09-16-trend-gate-index-and-alignment.md）：
  #   1. 原来那条「加闸门 7.4%/Sharpe0.57」是**未来函数**测出来的 —— 回测用**当日收盘**算 MA20，
  #      而闸门 09:31 执行，当时不可能知道当日收盘。口径对齐到可执行后，改善就消失了
  #      （pit 宇宙：熔断 −31.4% → 加闸门 −32.9%，Sharpe −0.36 → −0.73）。
  #   2. 配对 t 检验（239 个调仓期，同一批只差闸门一个开关）：**6/6 不显著**
  #      （t 在 −0.51 ~ +0.15，符号随宇宙/指数翻转）。累积不确定度 ±37~45pp，
  #      所以总收益上 10~25pp 的差异连一个标准误都不到。
  #   3. MA20 阻断率 51.1% —— 基本是一枚硬币，谈不上有区分度。
  # 关闭后失效的行为：跌破 MA20 的日子不再拦「开新仓」（api/main.py:1900、real_advice.py:164）。
  # 不受影响：加仓/减持、止损、回撤熔断、_market_weakness 弱势降仓（独立机制、无 enabled 开关），
  # 以及 selector 的市场状态权重（独立开关 selection.regime_gating，不读本节）。
  # [!] 注意 index 键只对线上闸门这一处生效；quant/data/selector.py:322 与
  #     scripts/40:152 仍硬编码 sh000300（属另一个机制，本次未动）。
  # 要重新评估请用 scripts/37（已支持 --index 与可执行口径），**别再引用上面那个旧数字**。
  market_trend_gate:
    enabled: false
    index: sh000300        # 仅 enabled: true 时生效
    ma_days: 20
  # 弱势主动减仓（2026-09-11）：大盘弱势/熔断时，把「超出目标仓位」的持仓减持到目标。
  # 原实现只"限制新买"、对已持有且超目标的只判 hold → 弱势日仓位一分不降（见 docs）。
  weak_trim: true
  # 追高/接飞刀保护：组合调仓「新买/加仓」前，按 现价vs昨收 的当日涨跌幅拦截
  chase_guard:
    enabled: true
    high_limit_pct: 2.0        # 涨幅 > 该值 → 追高，跳过买入（专治盘中买在最高点）
    drop_limit_pct: -3.0       # 跌幅 < 该值 → 急跌接飞刀，跳过买入
  # 回测式执行模式：'live'=现状(盘中实时价随时可调) | 'open'=仅开盘窗口可调、用参考价=昨收×(1+open_premium_pct)
  exec_mode: live
  open_window_minutes: 15      # open 模式下，开盘后前 N 分钟为可执行窗口（9:30 起）
  open_premium_pct: 0.5        # open 模式委托价相对昨收的溢价（%，近似开盘成交/回测口径）
  # 自动开盘执行：服务在开盘时刻(9:30+delay_sec)正运行时，自动执行一次组合调仓（每日一次）
  # 人工窗口易吃盘中波动（如东鹏开盘15分钟内大涨）——由机器在开盘瞬间自动触发更贴近开盘价。
  # 服务当时没开 → 当日不自动跑（符合“无法 24h 挂服务”约束）；手动“一键调仓”不受影响。
  auto_open_execute:
    enabled: true             # 服务在 9:31 前后运行 → 自动执行一次组合调仓（防手动错过开盘）
    delay_sec: 60              # 9:30 后延迟秒数再触发（留出集合竞价→连续竞价过渡）
    grace_min: 25              # 触发点过后仍允许补跑的分钟数（防线程启动略晚）
risk:
  enabled: true
  stop_loss_pct: 0.08
  take_profit_pct: 0.08   # 固定回退止盈（无 vol 时用）：保守 ≈+8% 落袋
  trailing_stop: true
  trailing_pct: 0.08
  # 按波动率动态止盈止损（ATR 自适应）：不同波动股票用不同宽窄的止损/止盈
  dynamic_volatility: true
  vol_window: 20          # ATR 计算窗口（日）
  atr_stop_mult: 3.0      # 止损 = 成本 - 3.0 × ATR（2.5 曾致高波动周期股被反复震出，放宽到 3）
  atr_take_mult: 2.5      # 止盈 = 成本 + 2.5 × ATR（保守锁盈：赢利 ≈+6~8% 即落袋）
  atr_trailing_mult: 2.5  # 移动止损 = 高点 - 2.5 × ATR
  vol_min_pct: 0.03       # 止损百分比下限（低波动股不设过窄）
  vol_max_pct: 0.15       # 止损百分比上限（高波动股不设过宽）
  take_min_pct: 0.05      # 止盈百分比下限
  take_max_pct: 0.30      # 止盈百分比上限
  # 长假前降仓：长假开始前的最后交易日自动把目标仓位上限降到 reduce_to_pct，规避跨长假跳空风险
  pre_holiday:
    enabled: true
    min_days_off: 3        # 自明日起连续休市(周末+法定假日) ≥ N 天 → 视为长假
    reduce_to_pct: 0.5     # 长假前目标仓位上限（0.5 = 50%）
    # ⭐ 当年 A股法定休市日（只列“非周末”的休市日即可，周末自动算），形如：
    #   holiday_dates: ["2026-10-01", "2026-10-02", ...]
    # 交易所每年末公布次年休市安排，需每年维护一次；留空则本功能不生效
    #（“未来”休市无法从行情推断，必须靠这份清单知道长假要来了）。
    holiday_dates: []
api:
  host: 127.0.0.1
  port: 8001
